
npm install

npx hardhat compile


to run local node:

npx hardhat node

to run local node with fork from rpc:

npx hardhat node --fork <http(s)://rpc_address.xyz>


to run tests:

npx hardhat test --network <network>




tested with node v.15.2.0 (stable release)
